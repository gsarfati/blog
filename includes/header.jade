header
  img(src="{{ site.logo | prepend: site.url }}")
  //.header-title {{ site.title }}
  .header-link

    | {% for page in site.pages %}
    | {% if page.title %}

    a.page-link(href="{{ page.url | prepend: site.baseurl }}") {{ page.title }}

    | {% endif %}
    | {% endfor %}
